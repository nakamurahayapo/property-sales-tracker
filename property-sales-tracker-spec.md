# 買取再販 物件販売管理アプリ 仕様書（Claudeコード引き継ぎ用）

## 概要
買取再販事業で買い取った物件の販売計画・価格推移をチームで共有管理するWebアプリ。
現行のArtifact版（HTML単体・ストレージ容量に上限あり）から、Firebase/Firestore + Vercel構成へ作り替える。

## 技術構成（他アプリと統一）
- フロントエンド：Web（Vercelデプロイ）
- データベース：Firebase Firestore
- 認証：Google Authentication（ドメイン制限）を想定 ※要確認
- GitHubリポジトリで管理

## データモデル（Firestore コレクション：properties）
| フィールド名 | 型 | 内容 |
|---|---|---|
| name | string | 物件名（必須） |
| staff | string | 担当名 |
| startPrice | number | 販売開始価格（万円） |
| currentPrice | number | 現在価格（万円） |
| grossProfit | number | 粗利（万円） |
| settlementDate | date | 決済日 |
| priceChangeDate | date | 価格変更日（直近の変更日） |
| history | array | 価格変更履歴。要素は `{ date, price }` の配列。現在価格を編集し値が変わるたびに自動で1件追加 |
| updatedAt | timestamp | 更新日時 |

## 機能要件
1. **一覧・CRUD**：物件の登録／編集／削除
2. **価格変更履歴の自動記録**：現在価格を変更して保存すると、その時点の価格変更日と価格を`history`に自動追加（手動入力欄は増やさない）
3. **履歴表示**：物件ごとに「履歴」ボタンから、価格推移の折れ線グラフ＋変更履歴の表（日付／価格／増減）を表示
4. **決済日アラート表示（色分け・通知は不要）**：
   - 決済日が今日より過去 → 行を赤色表示＋「期限超過」タグ
   - 決済日まで7日以内 → 行をゴールド表示＋「残り○日」タグ
5. **一覧グラフ**：物件別の粗利比較を横棒グラフで表示（フィルタ・検索結果と連動）
6. **フィルタ・検索**：担当名で絞り込み、物件名で検索
7. **ソート**：物件名／担当／各価格／決済日／価格変更日で並び替え可能（クリックで昇順・降順切り替え）
8. **サマリー表示**：物件数、合計粗利、決済まで7日以内の件数

## デザイン（いえプロ不動産ブランド準拠）
- ネイビー：#2C4A5E（メイン）／#3D5268（サブ）
- ゴールド：#D9922E（アクセント・CTA）
- 背景：#F1F4F6、サーフェス：#FFFFFF
- 危険色（期限超過）：#C0503F

## 現行Artifact版（参考実装）
下記URLの内容がそのまま機能仕様の参照実装。フィールド構成・履歴生成ロジック・グラフ描画も同じ挙動にする。
- ファイル：property-sales-tracker.html（このチャットで作成したもの）

## 未確定・要相談事項
- ログイン方式（Google認証をかけるか、社内アクセスのみで簡易にするか）
- 担当者ごとの合計粗利サマリーの要否
- 値下げが必要そうな物件を上位に出す並び替えロジックの要否
